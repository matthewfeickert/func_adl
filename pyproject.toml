[build-system]
requires = ["setuptools>=64", "setuptools_scm>=8"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_file = "src/func_adl/_version.py"
local_scheme = "no-local-version"

[tool.isort]
profile = "black"

[tool.black]
line-length = 99
